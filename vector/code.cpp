#include <iostream>
#include <vector>
using namespace std;

int main() {
    // vector<int> vec(5, 0);
    vector<char> vec = {'a', 'b', 'c', 'd', 'e'};
    
    // cout << "size = " << vec.size() << endl; // size function
    // vec.push_back(25); // push back function
    // cout << "after push back size = " << vec.size() << endl; // size function

    // cout << vec.front() << endl; // front function
    // cout << vec.back() << endl; // back function

    // vec.pop_back(); // pop back function
    // cout << "after push back size = " << vec.size() << endl; // size function

    // cout << vec.at(0) << endl; // at function using this function we can see the customer index value
    
    // cout<< vec[0] << endl;

    // vector<int> vec(3,0);

    // for(char i : vec) {
    //     cout << i << endl;
    // }


    // Staic vs Dynamic allocation.

     

    return 0;
}